#include "ExtractUbuntu.hpp"
#include <cstdlib>
#include <filesystem>
#include <iostream>

bool ExtractUbuntu::extractAll(const std::string &isoPath,
                               const std::string &workingDir,
                               const std::string &tftpDir,
                               const std::string &httpDir) {
  /*TODO: Similar to Arch, but with custom Ubuntu Logic
   */
  return false;
}
